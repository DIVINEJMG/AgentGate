import type{IntegrationAdapter}from'./contracts';const guarded=()=>{throw new Error('Generic REST / MCP is guarded until AgentGate has an explicit outbound-origin allowlist and egress validation boundary. Arbitrary URLs are not executable.')};export const genericMcpAdapter:IntegrationAdapter={provider:'generic_mcp',displayName:'Generic REST / MCP',credentialPolicy:'disabled',capabilities:[],supportedOperations:[],async validateConnection(){return guarded()},async checkHealth(){return guarded()},async executeOperation(){return guarded()}};
