# Aduoryn Python backend

This directory is the active Python backend introduced by F26.

F26.5 establishes structure and health/API-version scaffolding only. Domain behavior is migrated later by parity-tested foundations. The previous TypeScript backend is preserved under `reference/typescript-backend/` and on the `audoryn-f25-appdeploy-reference` checkpoint branch.
