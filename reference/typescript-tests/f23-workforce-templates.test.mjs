import assert from 'node:assert/strict';import test from 'node:test';import { getWorkforceTemplate,TEMPLATE_SECURITY_INVARIANTS,WORKFORCE_TEMPLATES } from '../backend/core/templateCatalog.ts';
test('F23 exposes the six roadmap workforce templates',()=>{assert.deepEqual(WORKFORCE_TEMPLATES.map((item)=>item.name),['Customer Support Assistant','Marketing Coordinator','Research Assistant','Sales Assistant','Operations Assistant','Developer Assistant'])});
test('every template carries the full SMB blueprint surface',()=>{for(const template of WORKFORCE_TEMPLATES){assert.ok(template.role.name);assert.ok(template.role.defaultResponsibilities.length);assert.ok(template.exampleJobs.length);assert.ok(template.requiredIntegrations.length);assert.ok(template.suggestedCapabilities.length);assert.ok(template.recommendedPolicyRules.length);assert.ok(Array.isArray(template.approvalDefaults));for(const job of template.exampleJobs){assert.ok(job.completionCriteria.length);assert.ok(job.responsibilityLinks.every((item)=>template.role.defaultResponsibilities.includes(item)))}}});
test('template security invariants forbid hidden authority',()=>{assert.deepEqual(TEMPLATE_SECURITY_INVARIANTS,{autoConnectIntegrations:false,autoDeclareCapabilities:false,autoCreatePolicies:false,autoApproveActions:false,activateWorker:false,activateJobs:false})});
test('write-capable templates recommend human approval for current F22 writes',()=>{const developer=getWorkforceTemplate('developer-assistant');const marketing=getWorkforceTemplate('marketing-coordinator');assert.ok(developer?.approvalDefaults.some((item)=>item.scope==='github.repository.issues.create'));assert.ok(marketing?.approvalDefaults.some((item)=>item.scope==='slack.messages.create'));assert.ok(developer?.recommendedPolicyRules.some((item)=>item.scope==='github.repository.issues.create'&&item.effect==='require_approval'))});
