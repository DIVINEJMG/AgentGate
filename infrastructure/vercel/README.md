# Vercel

Aduoryn's React/Vite frontend is deployed to Vercel from the GitHub repository.

The frontend is platform-neutral and communicates with the backend only through `VITE_API_BASE_URL` and the platform client boundary.

Render infrastructure is intentionally not created in F26.0–F26.5.
