import { useEffect,useMemo,useState } from 'react';import type { AuthUser } from '@appdeploy/client';import { Bot,Briefcase,Clock3,PauseCircle,PlayCircle,Plus,ShieldCheck,Users,UserRoundCog,X } from 'lucide-react';import type { OrganizationAccess } from '../lib/identityApi';import type { ApiVersion } from '../lib/systemApi';import { changeManagedWorkerStatus,createManagedWorker,createWorkforceRole,loadWorkforce,updateManagedWorker,updateWorkforceRole,type ManagedWorker,type RoleInput,type Weekday,type WorkforceRole,type WorkforceSnapshot,type WorkerInput,type WorkerStatus,type WorkingHours } from '../lib/workforceApi';
const DAYS:Weekday[]=['monday','tuesday','wednesday','thursday','friday','saturday','sunday'];function hours():WorkingHours{const timezone=Intl.DateTimeFormat().resolvedOptions().timeZone||'UTC';return{timezone,days:{monday:{enabled:true,start:'09:00',end:'17:00'},tuesday:{enabled:true,start:'09:00',end:'17:00'},wednesday:{enabled:true,start:'09:00',end:'17:00'},thursday:{enabled:true,start:'09:00',end:'17:00'},friday:{enabled:true,start:'09:00',end:'17:00'},saturday:{enabled:false,start:'09:00',end:'17:00'},sunday:{enabled:false,start:'09:00',end:'17:00'}}}}const EMPTY:WorkforceSnapshot={workers:[],roles:[],securityIdentities:[],summary:{workers:0,active:0,paused:0,suspended:0,roles:0,departments:0},window:{workersTruncated:false,rolesTruncated:false}};const ROLE_EMPTY:RoleInput={name:'',purpose:'',defaultInstructions:'',defaultResponsibilities:[],recommendedCapabilities:[]};
export default function WorkforcePanel({organization,user,apiVersion,onApiVersionChange,onNavigate}:{organization:OrganizationAccess;user:AuthUser;apiVersion:ApiVersion;onApiVersionChange:(version:ApiVersion)=>void;onNavigate:(view:'agents')=>void}){const[data,setData]=useState(EMPTY);const[selected,setSelected]=useState<ManagedWorker|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const[message,setMessage]=useState<string|null>(null);const[roleModal,setRoleModal]=useState(false);const[workerModal,setWorkerModal]=useState(false);const[editingRole,setEditingRole]=useState<WorkforceRole|null>(null);const[editingWorker,setEditingWorker]=useState<ManagedWorker|null>(null);const[roleForm,setRoleForm]=useState<RoleInput>(ROLE_EMPTY);const[workerForm,setWorkerForm]=useState<WorkerInput>({agentIdentityId:'',name:'',roleId:'',department:'',supervisorUserId:user.userId,description:'',responsibilities:[],instructions:'',workingHours:hours()});const[saving,setSaving]=useState(false);const canManage=organization.permissions.includes('workforce.manage');async function refresh(){setLoading(true);setError(null);try{const next=await loadWorkforce(apiVersion,organization.id);setData(next);setSelected((current)=>current?next.workers.find((worker)=>worker.id===current.id)??next.workers[0]??null:next.workers[0]??null)}catch(caught){setError(text(caught))}finally{setLoading(false)}}useEffect(()=>{void refresh()},[apiVersion,organization.id]);const availableAgents=useMemo(()=>data.securityIdentities.filter((identity)=>!identity.boundWorkerId||identity.boundWorkerId===editingWorker?.id),[data.securityIdentities,editingWorker]);function openRole(role?:WorkforceRole){setEditingRole(role??null);setRoleForm(role?{name:role.name,purpose:role.purpose,defaultInstructions:role.defaultInstructions,defaultResponsibilities:role.defaultResponsibilities,recommendedCapabilities:role.recommendedCapabilities}:ROLE_EMPTY);setRoleModal(true)}function openWorker(worker?:ManagedWorker){setEditingWorker(worker??null);setWorkerForm(worker?{agentIdentityId:worker.agentIdentityId,name:worker.name,roleId:worker.roleId,department:worker.department,supervisorUserId:worker.supervisorUserId,description:worker.description,responsibilities:worker.responsibilities,instructions:worker.instructions,workingHours:worker.workingHours}:{agentIdentityId:availableAgents[0]?.id??'',name:'',roleId:data.roles[0]?.id??'',department:'',supervisorUserId:user.userId,description:'',responsibilities:data.roles[0]?.defaultResponsibilities??[],instructions:data.roles[0]?.defaultInstructions??'',workingHours:hours()});setWorkerModal(true)}function lines(value:string){return value.split('\n').map((item)=>item.trim()).filter(Boolean)}async function saveRole(event:React.FormEvent){event.preventDefault();setSaving(true);setError(null);try{editingRole?await updateWorkforceRole(apiVersion,organization.id,editingRole.id,roleForm):await createWorkforceRole(apiVersion,organization.id,roleForm);setRoleModal(false);setMessage(editingRole?'Role updated.':'Role created.');await refresh()}catch(caught){setError(text(caught))}finally{setSaving(false)}}async function saveWorker(event:React.FormEvent){event.preventDefault();setSaving(true);setError(null);try{const worker=editingWorker?await updateManagedWorker(apiVersion,organization.id,editingWorker.id,workerForm):await createManagedWorker(apiVersion,organization.id,workerForm);setWorkerModal(false);setSelected(worker);setMessage(editingWorker?'Worker updated.':'Worker created as DRAFT.');await refresh()}catch(caught){setError(text(caught))}finally{setSaving(false)}}async function status(worker:ManagedWorker,next:WorkerStatus){if(next==='archived'&&!window.confirm(`Archive ${worker.name}? Archived workers cannot be restored.`))return;setSaving(true);setError(null);try{const updated=await changeManagedWorkerStatus(apiVersion,organization.id,worker.id,next);setSelected(updated);setMessage(`Worker status changed to ${next.toUpperCase()}.`);await refresh()}catch(caught){setError(text(caught))}finally{setSaving(false)}}function chooseRole(roleId:string){const role=data.roles.find((item)=>item.id===roleId);setWorkerForm((current)=>({...current,roleId,responsibilities:editingWorker?current.responsibilities:role?.defaultResponsibilities??[],instructions:editingWorker?current.instructions:role?.defaultInstructions??''}))}return <section className="workforce-page"><div className="page-heading"><div><p className="eyebrow">AI WORKFORCE · FOUNDATION 13</p><h1>Workers & roles</h1><p className="page-subtitle">Business-facing AI workers for {organization.name}, each anchored to a separate security Agent Identity.</p></div><div className="workforce-actions"><div className="api-switcher"><span>Compatibility view</span><div className="segmented"><button className={apiVersion==='v1'?'active':''} onClick={()=>onApiVersionChange('v1')}>API v1</button><button className={apiVersion==='v2'?'active':''} onClick={()=>onApiVersionChange('v2')}>API v2</button></div></div>{canManage&&<><button className="secondary-button" onClick={()=>openRole()}><Plus size={14}/> Role</button><button className="primary-button" onClick={()=>openWorker()} disabled={!data.roles.length||!availableAgents.length}><Plus size={14}/> Worker</button></>}</div></div><div className="workforce-boundary"><ShieldCheck size={17}/><div><strong>Management model only — autonomous runtime is not active yet.</strong><p>Worker status, instructions and role recommendations never grant capabilities or bypass Agent Identity, Policy, Risk, Approval or Incident Controls.</p></div></div>{error&&<div className="inline-error">{error}</div>}{message&&<div className="product-success">{message}</div>}<div className="audit-metrics"><Metric label="Workers" value={String(data.summary.workers)}/><Metric label="Active" value={String(data.summary.active)}/><Metric label="Roles" value={String(data.summary.roles)}/><Metric label="Departments" value={String(data.summary.departments)}/></div>{loading?<div className="audit-empty">Loading workforce…</div>:<><div className="workforce-layout"><div className="worker-directory"><div className="section-title"><div><p className="panel-kicker">WORKFORCE DIRECTORY</p><h2>Managed workers</h2></div><Users size={18}/></div>{data.workers.length===0?<div className="workforce-empty"><UserRoundCog size={24}/><strong>No managed workers yet</strong><p>Create a reusable role, register an Agent Identity, then bind the identity to a Worker.</p><div>{data.securityIdentities.length===0&&<button className="secondary-button" onClick={()=>onNavigate('agents')}>Register Agent Identity</button>}{canManage&&data.roles.length===0&&<button className="secondary-button" onClick={()=>openRole()}>Create first role</button>}</div></div>:data.workers.map((worker)=>{const role=data.roles.find((item)=>item.id===worker.roleId);return <button key={worker.id} className={`worker-row ${selected?.id===worker.id?'selected':''}`} onClick={()=>setSelected(worker)}><span className={`worker-avatar ${worker.status}`}><Bot size={16}/></span><div><strong>{worker.name}</strong><span>{role?.name??'Unresolved role'} · {worker.department}</span></div><span className={`status-chip ${worker.status==='active'?'success':'neutral'}`}>{worker.status.toUpperCase()}</span></button>})}</div><aside className="worker-profile">{selected?<WorkerProfile worker={selected} role={data.roles.find((item)=>item.id===selected.roleId)} identity={data.securityIdentities.find((item)=>item.id===selected.agentIdentityId)} currentUser={user} canManage={canManage} saving={saving} onEdit={()=>openWorker(selected)} onStatus={(next)=>void status(selected,next)}/>:<div className="audit-empty"><Briefcase size={22}/><strong>Select a worker</strong><span>Inspect role, supervisor, schedule, responsibilities and security binding.</span></div>}</aside></div><section className="roles-section"><div className="section-title"><div><p className="panel-kicker">ROLE CATALOG</p><h2>Reusable workforce roles</h2></div><Briefcase size={18}/></div><div className="role-grid">{data.roles.map((role)=><article className="role-card" key={role.id}><div><strong>{role.name}</strong>{canManage&&<button onClick={()=>openRole(role)}>Edit</button>}</div><p>{role.purpose}</p><span>{role.defaultResponsibilities.length} responsibilities · {role.recommendedCapabilities.length} recommended scopes</span></article>)}</div></section></>}{(data.window.workersTruncated||data.window.rolesTruncated)&&<p className="audit-window-note">The Workforce directory exceeded its bounded 100-record read window. Displayed totals may be conservative.</p>}{roleModal&&<div className="workforce-modal-backdrop"><form className="workforce-modal" onSubmit={saveRole}><header><div><p className="panel-kicker">WORKFORCE ROLE</p><h2>{editingRole?'Edit role':'Create role'}</h2></div><button type="button" onClick={()=>setRoleModal(false)}><X size={18}/></button></header><label>Role name<input required minLength={2} maxLength={80} value={roleForm.name} onChange={(e)=>setRoleForm({...roleForm,name:e.target.value})}/></label><label>Purpose<textarea required value={roleForm.purpose} onChange={(e)=>setRoleForm({...roleForm,purpose:e.target.value})}/></label><label>Default responsibilities<textarea placeholder="One responsibility per line" value={roleForm.defaultResponsibilities.join('\n')} onChange={(e)=>setRoleForm({...roleForm,defaultResponsibilities:lines(e.target.value)})}/></label><label>Default instructions<textarea value={roleForm.defaultInstructions} onChange={(e)=>setRoleForm({...roleForm,defaultInstructions:e.target.value})}/></label><label>Recommended capability scopes<textarea placeholder="One canonical scope per line — recommendations never grant access" value={roleForm.recommendedCapabilities.join('\n')} onChange={(e)=>setRoleForm({...roleForm,recommendedCapabilities:lines(e.target.value)})}/></label><footer><button type="button" className="ghost-button" onClick={()=>setRoleModal(false)}>Cancel</button><button className="primary-button" disabled={saving}>{saving?'Saving…':'Save role'}</button></footer></form></div>}{workerModal&&<div className="workforce-modal-backdrop"><form className="workforce-modal worker-form" onSubmit={saveWorker}><header><div><p className="panel-kicker">MANAGED WORKER</p><h2>{editingWorker?'Edit worker':'Create worker'}</h2></div><button type="button" onClick={()=>setWorkerModal(false)}><X size={18}/></button></header><div className="worker-form-grid"><label>Worker name<input required minLength={2} maxLength={80} value={workerForm.name} onChange={(e)=>setWorkerForm({...workerForm,name:e.target.value})}/></label><label>Agent Identity<select required disabled={Boolean(editingWorker)} value={workerForm.agentIdentityId} onChange={(e)=>setWorkerForm({...workerForm,agentIdentityId:e.target.value})}><option value="">Select identity</option>{availableAgents.map((identity)=><option key={identity.id} value={identity.id}>{identity.name} · {identity.status}</option>)}</select></label><label>Role<select required value={workerForm.roleId} onChange={(e)=>chooseRole(e.target.value)}><option value="">Select role</option>{data.roles.map((role)=><option key={role.id} value={role.id}>{role.name}</option>)}</select></label><label>Department<input required value={workerForm.department} onChange={(e)=>setWorkerForm({...workerForm,department:e.target.value})}/></label><label>Supervisor<input value={workerForm.supervisorUserId===user.userId?(user.name||user.email||'You'):workerForm.supervisorUserId} disabled/><small>F13 validates the stored supervisor as a same-workspace human member.</small></label><label>Timezone<input required value={workerForm.workingHours.timezone} onChange={(e)=>setWorkerForm({...workerForm,workingHours:{...workerForm.workingHours,timezone:e.target.value}})}/></label></div><label>Description<textarea value={workerForm.description} onChange={(e)=>setWorkerForm({...workerForm,description:e.target.value})}/></label><label>Responsibilities<textarea placeholder="One responsibility per line" value={workerForm.responsibilities.join('\n')} onChange={(e)=>setWorkerForm({...workerForm,responsibilities:lines(e.target.value)})}/></label><label>Worker instructions<textarea value={workerForm.instructions} onChange={(e)=>setWorkerForm({...workerForm,instructions:e.target.value})}/></label><div className="hours-editor"><strong>Working hours</strong>{DAYS.map((day)=>{const value=workerForm.workingHours.days[day];return <div key={day}><label><input type="checkbox" checked={value.enabled} onChange={(e)=>setWorkerForm({...workerForm,workingHours:{...workerForm.workingHours,days:{...workerForm.workingHours.days,[day]:{...value,enabled:e.target.checked}}}})}/><span>{day}</span></label><input type="time" disabled={!value.enabled} value={value.start} onChange={(e)=>setWorkerForm({...workerForm,workingHours:{...workerForm.workingHours,days:{...workerForm.workingHours.days,[day]:{...value,start:e.target.value}}}})}/><input type="time" disabled={!value.enabled} value={value.end} onChange={(e)=>setWorkerForm({...workerForm,workingHours:{...workerForm.workingHours,days:{...workerForm.workingHours.days,[day]:{...value,end:e.target.value}}}})}/></div>})}</div><footer><button type="button" className="ghost-button" onClick={()=>setWorkerModal(false)}>Cancel</button><button className="primary-button" disabled={saving}>{saving?'Saving…':editingWorker?'Save worker':'Create draft worker'}</button></footer></form></div>}</section>}
function WorkerProfile({worker,role,identity,currentUser,canManage,saving,onEdit,onStatus}:{worker:ManagedWorker;role?:WorkforceRole;identity?:{name:string;status:string;credentialStatus:string};currentUser:AuthUser;canManage:boolean;saving:boolean;onEdit:()=>void;onStatus:(status:WorkerStatus)=>void}){const enabled=Object.entries(worker.workingHours.days).filter(([,value])=>value.enabled);return <><div className="worker-profile-head"><span className={`worker-avatar large ${worker.status}`}><Bot size={20}/></span><div><p className="panel-kicker">MANAGED WORKER</p><h2>{worker.name}</h2><span>{role?.name??'Unresolved role'} · {worker.department}</span></div><span className={`status-chip ${worker.status==='active'?'success':'neutral'}`}>{worker.status.toUpperCase()}</span></div><div className="worker-profile-actions">{canManage&&worker.status!=='archived'&&<button className="secondary-button" onClick={onEdit}>Edit profile</button>}{canManage&&worker.status!=='active'&&worker.status!=='archived'&&<button className="secondary-button" disabled={saving} onClick={()=>onStatus('active')}><PlayCircle size={14}/> Activate</button>}{canManage&&worker.status==='active'&&<button className="secondary-button" disabled={saving} onClick={()=>onStatus('paused')}><PauseCircle size={14}/> Pause</button>}{canManage&&worker.status!=='suspended'&&worker.status!=='archived'&&<button className="ghost-button" disabled={saving} onClick={()=>onStatus('suspended')}>Suspend worker</button>}{canManage&&worker.status!=='archived'&&<button className="danger-button" disabled={saving} onClick={()=>onStatus('archived')}>Archive</button>}</div><dl className="worker-details"><div><dt>Supervisor</dt><dd>{worker.supervisorUserId===currentUser.userId?(currentUser.name||currentUser.email||'You'):worker.supervisorUserId}</dd></div><div><dt>Security identity</dt><dd>{identity?.name??worker.agentIdentityId}</dd></div><div><dt>Agent state</dt><dd>{identity?`${identity.status} · credential ${identity.credentialStatus}`:'Unavailable'}</dd></div><div><dt>Timezone</dt><dd>{worker.workingHours.timezone}</dd></div></dl><section className="worker-profile-section"><strong>Responsibilities</strong>{worker.responsibilities.length?<ul>{worker.responsibilities.map((item,index)=><li key={`${item}-${index}`}>{item}</li>)}</ul>:<p>No worker-specific responsibilities.</p>}</section><section className="worker-profile-section"><strong>Instructions</strong><p>{worker.instructions||'No worker-specific instructions.'}</p></section><section className="worker-profile-section"><strong>Working hours</strong><div className="schedule-chips">{enabled.length?enabled.map(([day,value])=><span key={day}><Clock3 size={12}/>{day.slice(0,3)} {value.start}–{value.end}</span>):<span>No working days enabled</span>}</div></section><section className="worker-security-note"><ShieldCheck size={15}/><p>Worker lifecycle is a workforce-management signal. Security execution remains governed by Agent Identity and Incident Controls.</p></section></>}
function Metric({label,value}:{label:string;value:string}){return <div className="audit-metric"><span>{label}</span><strong>{value}</strong></div>}function text(value:unknown){const data=value as{response?:{data?:{error?:string}};message?:string};return data.response?.data?.error||data.message||'Workforce request failed.'}
