import { Activity, Bot, CheckSquare2, FileKey2, KeyRound, LayoutDashboard, PlugZap, ScrollText, Settings2, ShieldAlert, ShieldCheck, X } from 'lucide-react';
export type AppView = 'overview' | 'agents' | 'integrations' | 'capabilities' | 'policies' | 'actions' | 'approvals' | 'audit' | 'risk' | 'incidents' | 'settings';
interface SidebarProps { open: boolean; onClose: () => void; activeView: AppView; onNavigate: (view: AppView) => void; }
export default function Sidebar({ open,onClose,activeView,onNavigate }: SidebarProps){function navigate(view:AppView){onNavigate(view);onClose()}return <><aside className={`sidebar ${open?'open':''}`} aria-label="Primary navigation"><div className="brand-block"><div className="brand-mark"><ShieldCheck size={17} strokeWidth={1.8}/></div><div className="brand-copy"><strong>AgentGate</strong><span>An SOT Product</span></div><button className="icon-button sidebar-close" aria-label="Close navigation" onClick={onClose}><X size={17}/></button></div><div className="nav-label">CONTROL PLANE</div><nav className="sidebar-nav"><button className={`nav-item ${activeView==='overview'?'active':''}`} onClick={()=>navigate('overview')}><LayoutDashboard size={16}/><span>Overview</span></button><button className={`nav-item ${activeView==='agents'?'active':''}`} onClick={()=>navigate('agents')}><Bot size={16}/><span>Agents</span></button><button className={`nav-item ${activeView==='integrations'?'active':''}`} onClick={()=>navigate('integrations')}><PlugZap size={16}/><span>Integrations</span></button><button className={`nav-item ${activeView==='capabilities'?'active':''}`} onClick={()=>navigate('capabilities')}><KeyRound size={16}/><span>Capabilities</span></button><button className={`nav-item ${activeView==='policies'?'active':''}`} onClick={()=>navigate('policies')}><FileKey2 size={16}/><span>Policies</span></button><button className={`nav-item ${activeView==='actions'?'active':''}`} onClick={()=>navigate('actions')}><Activity size={16}/><span>Actions</span></button><button className={`nav-item ${activeView==='approvals'?'active':''}`} onClick={()=>navigate('approvals')}><CheckSquare2 size={16}/><span>Approvals</span></button><button className={`nav-item ${activeView==='audit'?'active':''}`} onClick={()=>navigate('audit')}><ScrollText size={16}/><span>Audit</span></button><button className={`nav-item ${activeView==='risk'?'active':''}`} onClick={()=>navigate('risk')}><ShieldAlert size={16}/><span>Risk</span></button><button className={`nav-item ${activeView==='incidents'?'active':''}`} onClick={()=>navigate('incidents')}><Activity size={16}/><span>Incidents</span></button><button className={`nav-item ${activeView==='settings'?'active':''}`} onClick={()=>navigate('settings')}><Settings2 size={16}/><span>Settings</span></button></nav><div className="sidebar-foot"><p>Architecture track</p><strong>Modular monolith · v1.0</strong></div></aside>{open&&<button className="sidebar-overlay" aria-label="Close navigation overlay" onClick={onClose}/>}</>}
