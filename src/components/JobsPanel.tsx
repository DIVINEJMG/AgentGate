import { useEffect,useMemo,useState } from 'react';import { Archive,Briefcase,CheckCircle2,Clock3,ListTodo,PauseCircle,PlayCircle,Plus,ShieldCheck,X } from 'lucide-react';import type { OrganizationAccess } from '../lib/identityApi';import type { ApiVersion } from '../lib/systemApi';import { getAgentCapabilityProfile,listCapabilityCatalog,type AgentCapabilityProfile,type CapabilityCatalog } from '../lib/capabilityApi';import { loadWorkforce,type ManagedWorker,type WorkforceSnapshot } from '../lib/workforceApi';import { cancelWorkItem,changeJobStatus,createJob,loadJobsWorkspace,runJobNow,updateJob,type Job,type JobInput,type JobPriority,type JobsWorkspace,type WorkItem } from '../lib/jobsApi';
import SchedulerPanel from './SchedulerPanel';
const EMPTY:JobsWorkspace={jobs:[],workItems:[],jobSummary:{total:0,draft:0,active:0,paused:0,archived:0},queueSummary:{total:0,queued:0,cancelled:0,completed:0,failed:0},window:{jobsTruncated:false,workItemsTruncated:false}};const WORKFORCE_EMPTY:WorkforceSnapshot={workers:[],roles:[],securityIdentities:[],summary:{workers:0,active:0,paused:0,suspended:0,roles:0,departments:0},window:{workersTruncated:false,rolesTruncated:false}};const CATALOG_EMPTY:CapabilityCatalog={resources:[],scopes:[],summary:{resources:0,actions:0,scopes:0}};const INPUT:JobInput={workerId:'',name:'',description:'',objective:'',instructions:'',responsibilityLinks:[],requiredCapabilities:[],priority:'normal',completionCriteria:[]};
export default function JobsPanel({organization,apiVersion,onApiVersionChange}:{organization:OrganizationAccess;apiVersion:ApiVersion;onApiVersionChange:(version:ApiVersion)=>void}){const[data,setData]=useState(EMPTY);const[workforce,setWorkforce]=useState(WORKFORCE_EMPTY);const[catalog,setCatalog]=useState(CATALOG_EMPTY);const[selected,setSelected]=useState<Job|null>(null);const[profile,setProfile]=useState<AgentCapabilityProfile|null>(null);const[tab,setTab]=useState<'jobs'|'queue'|'triggers'>('jobs');const[modal,setModal]=useState(false);const[editing,setEditing]=useState<Job|null>(null);const[form,setForm]=useState<JobInput>(INPUT);const[loading,setLoading]=useState(true);const[saving,setSaving]=useState(false);const[error,setError]=useState<string|null>(null);const[message,setMessage]=useState<string|null>(null);const canManage=organization.permissions.includes('jobs.manage');const canRun=organization.permissions.includes('jobs.run');async function refresh(){setLoading(true);setError(null);try{const[next,workers,capabilities]=await Promise.all([loadJobsWorkspace(apiVersion,organization.id),loadWorkforce(apiVersion,organization.id),listCapabilityCatalog(apiVersion,organization.id)]);setData(next);setWorkforce(workers);setCatalog(capabilities);setSelected((current)=>current?next.jobs.find((job)=>job.id===current.id)??next.jobs[0]??null:next.jobs[0]??null)}catch(caught){setError(text(caught))}finally{setLoading(false)}}useEffect(()=>{void refresh()},[apiVersion,organization.id]);useEffect(()=>{const worker=selected?workforce.workers.find((item)=>item.id===selected.workerId):null;if(!worker){setProfile(null);return}void getAgentCapabilityProfile(apiVersion,organization.id,worker.agentIdentityId).then(setProfile).catch(()=>setProfile(null))},[selected?.id,workforce.workers,apiVersion,organization.id]);const activeWorkers=workforce.workers.filter((worker)=>worker.status!=='archived');const chosenWorker=workforce.workers.find((worker)=>worker.id===form.workerId);const history=useMemo(()=>selected?data.workItems.filter((item)=>item.jobId===selected.id):[],[selected,data.workItems]);function openJob(job?:Job){setEditing(job??null);setForm(job?{workerId:job.workerId,name:job.name,description:job.description,objective:job.objective,instructions:job.instructions,responsibilityLinks:job.responsibilityLinks,requiredCapabilities:job.requiredCapabilities,priority:job.priority,completionCriteria:job.completionCriteria}:{...INPUT,workerId:activeWorkers[0]?.id??''});setModal(true)}function lines(value:string){return value.split('\n').map((item)=>item.trim()).filter(Boolean)}function toggle(field:'responsibilityLinks'|'requiredCapabilities',value:string){setForm((current)=>({...current,[field]:current[field].includes(value)?current[field].filter((item)=>item!==value):[...current[field],value]}))}async function save(event:React.FormEvent){event.preventDefault();setSaving(true);setError(null);try{const job=editing?await updateJob(apiVersion,organization.id,editing.id,form):await createJob(apiVersion,organization.id,form);setModal(false);setSelected(job);setMessage(editing?`Job updated to revision ${job.revision}.`:'Job created as DRAFT.');await refresh()}catch(caught){setError(text(caught))}finally{setSaving(false)}}async function status(job:Job,next:'active'|'paused'|'archived'){if(next==='archived'&&!window.confirm(`Archive ${job.name}? Archived Jobs cannot be restored.`))return;setSaving(true);setError(null);try{const updated=await changeJobStatus(apiVersion,organization.id,job.id,next);setSelected(updated);setMessage(`Job is now ${next.toUpperCase()}.`);await refresh()}catch(caught){setError(text(caught))}finally{setSaving(false)}}async function queue(job:Job){setSaving(true);setError(null);try{const item=await runJobNow(apiVersion,organization.id,job.id);setMessage(`Work Item ${item.id.slice(0,8)} queued. Managed Runtime is still intentionally inactive until F16.`);setTab('queue');await refresh()}catch(caught){setError(text(caught))}finally{setSaving(false)}}async function cancel(item:WorkItem){setSaving(true);setError(null);try{await cancelWorkItem(apiVersion,organization.id,item.id);setMessage('Queued Work Item cancelled.');await refresh()}catch(caught){setError(text(caught))}finally{setSaving(false)}}const selectedWorker=selected?workforce.workers.find((worker)=>worker.id===selected.workerId):null;const missing=selected&&profile?selected.requiredCapabilities.filter((scope)=>!profile.activeScopes.includes(scope)):selected?.requiredCapabilities??[];const ready=Boolean(selected&&selectedWorker?.status==='active'&&profile?.agent.status==='active'&&missing.length===0);return <section className="jobs-page"><div className="page-heading"><div><p className="eyebrow">JOBS, WORK QUEUE & TRIGGERS · FOUNDATION 15</p><h1>Define work. Queue it safely.</h1><p className="page-subtitle">Jobs turn Worker responsibilities into durable work definitions without bypassing the AgentGate control plane.</p></div><div className="jobs-actions"><div className="api-switcher"><span>Compatibility view</span><div className="segmented"><button className={apiVersion==='v1'?'active':''} onClick={()=>onApiVersionChange('v1')}>API v1</button><button className={apiVersion==='v2'?'active':''} onClick={()=>onApiVersionChange('v2')}>API v2</button></div></div>{canManage&&<button className="primary-button" onClick={()=>openJob()} disabled={!activeWorkers.length}><Plus size={14}/> Job</button>}</div></div><div className="jobs-boundary"><ShieldCheck size={17}/><div><strong>Manual and automatic triggers queue work; they do not execute AI work in Foundation 15.</strong><p>The Scheduler and Trigger Engine are active now. Planner, Run Engine and Step Executor arrive in F16, so external actions still cannot originate from this queue yet.</p></div></div>{error&&<div className="inline-error">{error}</div>}{message&&<div className="product-success">{message}</div>}<div className="audit-metrics"><Metric label="Jobs" value={String(data.jobSummary.total)}/><Metric label="Active" value={String(data.jobSummary.active)}/><Metric label="Queued work" value={String(data.queueSummary.queued)}/><Metric label="History" value={String(data.queueSummary.total)}/></div><div className="jobs-tabs"><button className={tab==='jobs'?'active':''} onClick={()=>setTab('jobs')}><Briefcase size={14}/> Jobs</button><button className={tab==='queue'?'active':''} onClick={()=>setTab('queue')}><ListTodo size={14}/> Work Queue <span>{data.queueSummary.queued}</span></button><button className={tab==='triggers'?'active':''} onClick={()=>setTab('triggers')}><Clock3 size={14}/> Triggers</button></div>{loading?<div className="audit-empty">Loading Jobs & Work Queue…</div>:tab==='triggers'?<SchedulerPanel organization={organization} apiVersion={apiVersion} jobs={data.jobs} workers={workforce.workers} onWorkChanged={()=>void refresh()}/>:tab==='jobs'?<div className="jobs-layout"><div className="job-list">{data.jobs.length===0?<div className="jobs-empty"><Briefcase size={24}/><strong>No Jobs yet</strong><p>Create a Job for one of your managed Workers. It will begin as DRAFT.</p></div>:data.jobs.map((job)=>{const worker=workforce.workers.find((item)=>item.id===job.workerId);return <button className={`job-row ${selected?.id===job.id?'selected':''}`} key={job.id} onClick={()=>setSelected(job)}><span className={`priority-dot ${job.priority}`}/><div><strong>{job.name}</strong><span>{worker?.name??'Unknown Worker'} · rev {job.revision} · {job.priority}</span></div><span className={`status-chip ${job.status==='active'?'success':'neutral'}`}>{job.status.toUpperCase()}</span></button>})}</div><aside className="job-detail">{selected?<><div className="job-detail-head"><div><p className="panel-kicker">JOB DEFINITION</p><h2>{selected.name}</h2><span>{selectedWorker?.name??selected.workerId} · revision {selected.revision}</span></div><span className={`status-chip ${selected.status==='active'?'success':'neutral'}`}>{selected.status.toUpperCase()}</span></div><div className="job-controls">{canManage&&selected.status!=='archived'&&<button className="secondary-button" onClick={()=>openJob(selected)}>Edit</button>}{canManage&&(selected.status==='draft'||selected.status==='paused')&&<button className="secondary-button" disabled={saving} onClick={()=>void status(selected,'active')}><PlayCircle size={14}/>{selected.status==='paused'?'Resume':'Activate'}</button>}{canManage&&selected.status==='active'&&<button className="secondary-button" disabled={saving} onClick={()=>void status(selected,'paused')}><PauseCircle size={14}/> Pause</button>}{canRun&&selected.status==='active'&&<button className="primary-button" disabled={saving} onClick={()=>void queue(selected)}><PlayCircle size={14}/> Run now</button>}{canManage&&selected.status!=='archived'&&<button className="ghost-button" disabled={saving} onClick={()=>void status(selected,'archived')}><Archive size={14}/> Archive</button>}</div><div className={`job-readiness ${ready?'ready':'not-ready'}`}><CheckCircle2 size={15}/><div><strong>{ready?'Ready to queue':'Readiness gate'}</strong><span>{selectedWorker?.status!=='active'?'Worker must be ACTIVE.':profile?.agent.status!=='active'?'Agent Identity must be ACTIVE.':missing.length?`Missing active capability: ${missing[0]}`:'Requirements will be rechecked server-side.'}</span></div></div><dl className="job-meta"><div><dt>Priority</dt><dd>{selected.priority}</dd></div><div><dt>Trigger</dt><dd>Manual + configurable F15 triggers</dd></div></dl><Block title="Objective" text={selected.objective}/><Block title="Instructions" text={selected.instructions||'No job-specific instructions.'}/><ListBlock title="Responsibility linkage" items={selected.responsibilityLinks}/><ListBlock title="Required capabilities" items={selected.requiredCapabilities} empty="No external capability requirement."/><ListBlock title="Completion criteria" items={selected.completionCriteria}/><section className="job-history"><strong>Job history</strong>{history.length===0?<p>No Work Items have been queued for this Job.</p>:history.slice(0,6).map((item)=><div key={item.id}><span className={`queue-status ${item.status}`}>{item.status}</span><span>{new Date(item.createdAt).toLocaleString()}</span><code>{item.correlationId}</code></div>)}</section></>:<div className="jobs-empty"><ListTodo size={22}/><strong>Select a Job</strong><p>Inspect objective, requirements, revision and queue history.</p></div>}</aside></div>:<Queue items={data.workItems} jobs={data.jobs} workers={workforce.workers} canRun={canRun} saving={saving} onCancel={(item)=>void cancel(item)}/>} {(data.window.jobsTruncated||data.window.workItemsTruncated)&&<p className="audit-window-note">The bounded Jobs/Queue read window is truncated. No totals beyond the scanned window are being invented.</p>}{modal&&<div className="workforce-modal-backdrop"><form className="workforce-modal job-form" onSubmit={save}><header><div><p className="panel-kicker">JOB DEFINITION</p><h2>{editing?'Edit Job':'Create Job'}</h2></div><button type="button" onClick={()=>setModal(false)}><X size={18}/></button></header><div className="job-form-grid"><label>Worker<select required disabled={Boolean(editing)} value={form.workerId} onChange={(e)=>setForm({...form,workerId:e.target.value,responsibilityLinks:[]})}><option value="">Select Worker</option>{activeWorkers.map((worker)=><option value={worker.id} key={worker.id}>{worker.name} · {worker.status}</option>)}</select></label><label>Priority<select value={form.priority} onChange={(e)=>setForm({...form,priority:e.target.value as JobPriority})}><option value="low">Low</option><option value="normal">Normal</option><option value="high">High</option><option value="urgent">Urgent</option></select></label></div><label>Job name<input required minLength={2} maxLength={100} value={form.name} onChange={(e)=>setForm({...form,name:e.target.value})}/></label><label>Description<textarea value={form.description} onChange={(e)=>setForm({...form,description:e.target.value})}/></label><label>Objective<textarea required value={form.objective} onChange={(e)=>setForm({...form,objective:e.target.value})}/></label><label>Job instructions<textarea value={form.instructions} onChange={(e)=>setForm({...form,instructions:e.target.value})}/></label><fieldset><legend>Responsibility linkage</legend><small>Links this Job to the Worker charter; it does not change the charter.</small><div className="job-check-grid">{chosenWorker?.responsibilities.length?chosenWorker.responsibilities.map((item)=><label key={item}><input type="checkbox" checked={form.responsibilityLinks.includes(item)} onChange={()=>toggle('responsibilityLinks',item)}/><span>{item}</span></label>):<p>No responsibilities are available for this Worker.</p>}</div></fieldset><fieldset><legend>Required capabilities</legend><small>Requirements never grant scopes. Activation and Run now require these scopes to be active on the bound Agent.</small><div className="job-check-grid">{catalog.scopes.length?catalog.scopes.map((scope)=><label key={scope}><input type="checkbox" checked={form.requiredCapabilities.includes(scope)} onChange={()=>toggle('requiredCapabilities',scope)}/><code>{scope}</code></label>):<p>No live capability scopes are currently available.</p>}</div></fieldset><label>Completion criteria<textarea required placeholder="One completion condition per line" value={form.completionCriteria.join('\n')} onChange={(e)=>setForm({...form,completionCriteria:lines(e.target.value)})}/></label><footer><button type="button" className="ghost-button" onClick={()=>setModal(false)}>Cancel</button><button className="primary-button" disabled={saving}>{saving?'Saving…':editing?'Save Job':'Create draft Job'}</button></footer></form></div>}</section>}
function Queue({items,jobs,workers,canRun,saving,onCancel}:{items:WorkItem[];jobs:Job[];workers:ManagedWorker[];canRun:boolean;saving:boolean;onCancel:(item:WorkItem)=>void}){return <div className="queue-panel"><div className="section-title"><div><p className="panel-kicker">DURABLE WORK QUEUE</p><h2>Work Items</h2></div><ListTodo size={18}/></div>{items.length===0?<div className="jobs-empty"><Clock3 size={24}/><strong>Queue is empty</strong><p>Use Run now on an ACTIVE Job to create a durable Work Item.</p></div>:items.map((item)=>{const job=jobs.find((entry)=>entry.id===item.jobId);const worker=workers.find((entry)=>entry.id===item.workerId);return <article className="queue-row" key={item.id}><div className="queue-row-main"><span className={`queue-status ${item.status}`}>{item.status.toUpperCase()}</span><div><strong>{job?.name??item.snapshot.jobName}</strong><span>{worker?.name??item.workerId} · job rev {item.jobRevision} · {item.priority}</span></div></div><div className="queue-row-meta"><span>{new Date(item.createdAt).toLocaleString()}</span><code>{item.correlationId}</code>{item.runId?<span>Run {item.runId}</span>:<span>Run not created yet</span>}{canRun&&item.status==='queued'&&<button className="ghost-button" disabled={saving} onClick={()=>onCancel(item)}>Cancel</button>}</div></article>})}</div>}
function Block({title,text}:{title:string;text:string}){return <section className="job-block"><strong>{title}</strong><p>{text}</p></section>}function ListBlock({title,items,empty='None linked.'}:{title:string;items:string[];empty?:string}){return <section className="job-block"><strong>{title}</strong>{items.length?<ul>{items.map((item)=><li key={item}>{item}</li>)}</ul>:<p>{empty}</p>}</section>}function Metric({label,value}:{label:string;value:string}){return <div className="audit-metric"><span>{label}</span><strong>{value}</strong></div>}function text(value:unknown){const data=value as{response?:{data?:{error?:string}};message?:string};return data.response?.data?.error||data.message||'Jobs request failed.'}
